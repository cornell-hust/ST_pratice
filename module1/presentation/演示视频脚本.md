# 模块一演示视频脚本（待录制）

> 本文件为 5–8 分钟录制脚本，当前未录制真实视频。

## 0:00–0:40 环境与对象
进入 module1，说明 croniter 6.2.2 基线和测试范围。展示 README 与目录。

## 0:40–1:30 测试策略
说明 36 条用例来自等价类、边界值、场景与状态转换，自动化比例超过 80%。展示 Excel 清单中的编号和方法字段。

## 1:30–2:30 一键执行
运行 `python -m pytest -q`，停留在 `41 passed`。说明测试数据、fixture 和 pytest 标记。

## 2:30–4:30 缺陷复现与修复
以零步长表达式 `5-5/0` 为例：先展示 6.2.2 基线行为，再切换修复提交 `07dfad8`，运行回归用例并展示稳定异常。随后口头说明月份边界修复 `101f39e` 与 Sunday 修复 `10e9bb6`，打开 repro-fixed.txt。

## 4:30–5:30 结果与证据
展示 `41 passed`、覆盖率 55% 和三个缺陷报告。明确覆盖率是当前测量结果，不宣称达到目标。

## 5:30–6:30 交付物与结论
依次展示附录1清单、附录2缺陷报告、附录3测试报告和 Git 提交记录。结束时说明所有结果可在本地命令复现。
