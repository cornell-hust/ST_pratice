# presentation/ — 演示与汇报材料

| 文件/目录 | 作用 |
|---|---|
| `output/模块一成果汇报v2.pptx` | **现行汇报稿**（12 页，基于 `module1/华科ppt母版.pptx` 生成，含最新 40 条用例口径） |
| `演示视频脚本.md` | 演示视频录制脚本（数字待同步为 40 条用例口径） |

说明与约定：

- 最终产物放 `output/`；旧版汇报稿（36 条口径）已于 2026-09-11 清理，仅保留现行 v2；
- **汇报稿 v2 的素材来源**：封面右上角徽标取自华中科技大学软件学院官网（https://sse.hust.edu.cn/ 招生横幅中的院徽，裁切为透明底圆盘）；母版其余版式（校徽、校名）保持 `module1/华科ppt母版.pptx` 原样，母版文件本身未做修改；
- 原 `build/` 目录（演示稿构建脚本 `build.mjs` 与误提交的 `node_modules` 文本文件）已于 2026-09-11 清理：构建脚本硬编码依赖创建者 macOS 上的 Codex 运行时，在其他机器无法运行；现行汇报稿 v2 由 Python（python-pptx）流程直接生成，不再需要该脚本。
