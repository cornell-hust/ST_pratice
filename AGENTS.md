# 项目协作说明

本文件是 `ST_pratice` 项目的本地协作约定，适用于项目根目录及其子目录。

## 项目基本信息

- 项目用途：软件测试实践课程资料、作业与相关文档维护。
- 默认仓库分支：`main`。
- 远程仓库：`git@github.com:cornell-hust/ST_pratice.git`。
- 项目文档以中文为主；新增或修改面向课程使用者的内容时，优先使用简体中文和清晰的 Markdown 结构。

## 修改与提交规范

1. 开始修改前先检查工作区状态，保护已有的用户修改，不覆盖、不回退与当前任务无关的变更。
2. 每次完成一组逻辑完整的修改，都必须提交到本地 Git；不要把多个互不相关的任务混在同一个提交中。
   - 仅提交本次任务涉及的文件；逐项检查暂存区，保留用户已有的暂存内容，避免将无关改动一并提交。
   - 提交描述应包含修改内容、修改原因和验证结果；完成后向用户报告本地 commit 和涉及的文件。
3. 提交前进行与改动相匹配的验证（例如文档链接检查、格式检查、构建或测试），并在提交说明或最终报告中记录验证结果。
4. Commit message 使用清楚、具体、可检索的描述，说明“改了什么以及为什么”。推荐格式：

   ```text
   <type>(<scope>): <简明变更说明>

   <必要时补充背景、主要内容和验证方式>
   ```

   常用 `type`：`docs`（文档）、`feat`（功能）、`fix`（修复）、`test`（测试）、`chore`（维护）。
5. 除非用户明确要求，不自动执行 `git push`、合并分支、创建 Pull Request 或修改远程仓库。

## 推送到 GitHub 的规范

当用户明确要求 push 到 GitHub 时：

1. 先确认待推送的分支、提交范围、远程地址和工作区状态，避免把无关或未确认的文件带入推送。
2. 推送前给出充分、准确的变更说明，至少包括：背景/目标、主要改动、验证结果、已知限制或风险，以及将要推送的分支和提交。
3. 推送完成后报告实际推送的远程、分支和 commit；如果需要 Pull Request，使用同样完整的信息撰写 PR 标题和描述。
4. 不在提交、日志、文档或命令输出中写入密码、令牌、私钥等敏感信息。

推送说明使用以下模板，按实际变更填写，不编造验证结果：

```text
背景与目标：为什么需要这次修改。
主要改动：涉及的文件、具体修改及影响。
验证结果：执行的检查、结果，以及未验证的内容。
限制与风险：已知问题或注意事项；没有已知问题时如实说明。
推送范围：实际远程地址、分支、提交范围及各提交摘要。
```

目标仓库为 `git@github.com:cornell-hust/ST_pratice.git`；指向同一仓库的 HTTPS 地址也可使用。推送前通过 Git 检查实际远程配置。详细说明写在提交信息及面向用户的推送报告中；仅在用户要求创建 Pull Request 时，另行填写 PR 描述。

## 工作方式

- 优先做小而明确、可回滚的修改；复用项目已有格式和工具。
- 对音频、PDF、Word、Excel 等课程资料进行转换时，保留原文件，不删除源材料；输出文件应注明来源或用途。
- 对录音转写内容，区分原始转写与整理后的摘要/笔记，不把推断内容伪装成录音原话。
- 完成任务前检查 `git diff`、相关测试或文档渲染结果，并在最终说明中列出改动文件和验证情况。
