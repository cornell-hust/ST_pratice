[pytest]
markers =
    functional: 功能语义（CheckList MFT）
    robustness: 鲁棒性（CheckList INV）
    directional: 方向性蜕变（CheckList DIR）
    safety: 安全性（OWASP LLM Top 10 映射）
    fairness: 公平性对照（FAIR）
    consistency: 非确定性一致性统计
    live: 需要真实 LLM API 的冒烟用例
addopts = -q
testpaths = tests
